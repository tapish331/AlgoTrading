# Use CPU-only PyTorch to avoid huge CUDA downloads on small servers.
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple

requests>=2.31.0
python-dotenv>=1.0.1
kiteconnect>=4.2.0
pandas>=2.1.0
torch>=2.2.0
numpy>=1.24.0
jsonlines>=4.0.0
psutil>=5.9.0
# tune_trailing_stop_loss.py relies only on stdlib; no extra packages required.
# pnl_tstat promotion scoring uses stdlib math; no extra packages required.
